import React from "react";
import "../App.css";

const ErrorScreen = () => {
  return (
    <h1 className="error-screen-message">
      "Error: Problem loading Google Maps API"
    </h1>
  );
};

export default ErrorScreen;
